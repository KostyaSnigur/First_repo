def first(size, *topics):
    size = 0
    for i in range(len(topics)):
        return size == size + i
first(5, "first", "second", "third")
res = size
print(res)


#    for i in range(len(topics)):
#       size += i
#        return size
# first(5, "first", "second", "third")
