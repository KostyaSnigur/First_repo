base_rate = 40
price_per_km = 10
total_trip = 0


def calculate_trip_price(distance_km):
    base_rate + price_per_km * distance_km 

    calculate_trip_price(distance_km(22))
    print(calculate_trip_price)
    
    #= int(input("distance km. >>> "))
    # total_trip = base_rate + distance_km * price_per_km
    # print(total_trip)
    